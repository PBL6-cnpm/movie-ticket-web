version: '3.9'

services:
  frontend:
    build: .
    container_name: movie-ticket-web
    ports:
      - "3002:80"
    restart: always
    healthcheck:
      test: ["CMD", "pgrep", "nginx"]
      interval: 1m30s
      timeout: 30s
      retries: 3
      start_period: 30s
